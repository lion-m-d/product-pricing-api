package com.ws.domain.price.model;

/**
 * Represents a brand with an ID and a name.
 *
 * @param id   the brand's unique identifier
 * @param name the brand's name
 */
public record Brand(Integer id, String name) {}